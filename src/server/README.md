# server

A library using `poll`to create a basic server.

## Usage

To use the library define `PQD_SERVER_IMPLEMENTATION` and
then include the `pqd_server.h` file.  

``` C
#define PQD_SERVER_IMPLEMENTATION
#include "pqd_server.h"

int main(){

    return 0;
}
```
